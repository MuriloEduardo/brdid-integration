/**
 * Controller de clientes removido
 * 
 * A API BRDID gerencia clientes dentro do módulo Billing.
 * Veja billing.controller.js para endpoints de clientes.
 * 
 * Endpoints disponíveis em /api/billing:
 * - POST /billing/clientes (criar cliente)
 * - GET /billing/clientes (listar clientes)
 * - POST /billing/vincular (vincular DIDs e plano a cliente)
 */

module.exports = {};
