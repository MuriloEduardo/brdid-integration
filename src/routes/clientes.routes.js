const express = require('express');
const router = express.Router();

/**
 * Routes de clientes removidas.
 * A API BRDID gerencia clientes dentro do módulo Billing.
 * Veja billing.routes.js para endpoints de clientes.
 */

module.exports = router;
